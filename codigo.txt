int x, y;
float res;
bool x;

function float division(int x, int y)
float res, total;
{
  res = x / y;
  return res;
}

function int modulo(int x, int y)
float res;
{
  res = x \ y;
  return res;
}

main()
{
  x = 8;
  y = 3;
  res = call division(x, y);
  write(res);
  res = call modulo(x, y);
  write(res);
  write(total);
}